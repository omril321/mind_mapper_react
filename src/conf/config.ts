const config = {
    /**
     * minimum_occurrences_for_combining_entities: The minimal occurrence number of an entity, for it to be combined with any other entity.
     */
    minimum_occurrences_for_combining_entities: 3,
    /**
     * minimum_occurrences_for_entity_to_be_meaningful: The minimal occurrence number of an entity,
     * for it to appear in any output of the entity extraction algorithm
     */
    minimum_occurrences_for_entity_to_be_meaningful: 2,
};

export default config;
